# 徐海学院抢课脚本 · How To Use

> ⚠️ **免责声明**
> 请勿将此程序用于恶意抢课盈利行为，恶意黄牛行为造成的后果本人概不负责。
> 下载并使用本仓库程序则代表您自愿承担一切责任。
> 如您对此免责声明有异议或出于某种原因不愿遵守，请勿使用本程序。
> （完整版见 [DISCLAIMER.md](DISCLAIMER.md)）

命令行版（`grab.py`）的快速上手，四步。图形界面版见文末。

---

## 1. 安装依赖包

**不需要装。** 只用 Python 标准库，没有任何第三方依赖。

唯一要求：装好 Python 3，安装时勾上 `Add Python to PATH`。

> 例外：图形界面里的「浏览器点击」方式需要 selenium。
> 项目里已经附带在 `libs/`，缺失时启动脚本会自动装，不用手动管。
> **高峰期用的「高速直投」完全不需要 selenium。**

---

## 2. 修改 config.json

把 `config.example.json` 复制一份改名成 `config.json`，填三项：

```json
{
  "user": "20260001",
  "password": "你的教务密码",
  "course": ["3F0A1B2C", "7B2E9D01"]
}
```

- `user` —— 学号
- `password` —— 教务系统密码（正方不需要验证码）
- `course` —— 要抢的**教学班 jxb_id** 列表，多个就写多个，抢到任意一个即停

**不知道 jxb_id？** 先列一下可选课程：

```bash
python grab.py --config config.json --list
```

输出里每门课都会带自己的 `jxb_id`，复制过来即可。

> `config.json` 里有明文密码，不要发给别人、不要传 Git。
> 已经加了 `.gitignore` 的话不用管；没有的话自己注意。

---

## 3. 启动程序

```bash
python grab.py --config config.json
```

会依次：登录 → 找选课页 → 匹配目标课 → 开始轮询提交，抢到就停。

**抢课高峰期建议加上 `--at`，让它到点自动开抢**：

```bash
python grab.py --config config.json --at 12:30:00
```

程序会先登录就位、把目标课定位好，然后等到 12:30 那一刻开始连续提交。
高峰期请**提前几分钟**跑起来，别等开放了才启动。

---

## 4. 常用参数（可选）

| 参数 | 作用 |
|---|---|
| `--list` | 列出可选课程及 jxb_id |
| `--dry-run` | 只登录 + 定位，不真正提交（先验证配置写对没有） |
| `--at 12:30:00` | 等到指定时刻再开始抢 |
| `--interval 0.35` | 每轮间隔秒数（默认 0.35，别设成 0） |
| `--minutes 30` | 最长跑多少分钟，0 = 不限 |
| `--course 3F0A1B2C` | 临时指定课程，不用改 config.json |

**第一次用建议先跑一次 `--dry-run`**：

```bash
python grab.py --config config.json --dry-run
```

能打印出"登录成功 + 找到选课页 + 匹配到你的课"就说明配置没问题，
到时候直接去掉 `--dry-run` 用。

---

## 5. 图形界面版（更简单，推荐）

**双击 `启动抢课助手.bat`**

然后：填学号密码 → 登录 → 在读出的课程表里**双击**想抢的课 →
确认勾着「高峰期模式」→ 点【▶ 开始抢课】。

细节和常见问题看同目录的 **`使用说明.txt`**。
